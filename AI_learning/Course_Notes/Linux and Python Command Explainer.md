## FIND  AND  >DEV>NULL 
find ~ -name "pipeline_starter.py" 2>/dev/null
- `find` – Linux utility to search for files and directories.
- `~` – Your home directory (e.g., `/home/prahnesh`).
- `-name "pipeline_starter.py"` – Look for files or directories whose name exactly matches `pipeline_starter.py`.
-